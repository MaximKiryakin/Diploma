=== TODO: МАГИСТЕРСКАЯ ДИССЕРТАЦИЯ ===

--- P0 (КРИТИЧЕСКИЕ) ---
- [x] Исправить "Курсовая работа" -> "Магистерская диссертация" в text/title.tex (строка 25)
- [ ] Расширить Главу 4: добавить таблицы результатов бэктеста всех 3 стратегий (Mean-EL, CVaR, Risk Parity)
- [ ] Расширить Главу 4: добавить результаты amount-based оптимизации (RAROC, одобренные/отклоненные заявки)
- [ ] Расширить Главу 4: добавить графики weight_history, strategy_backtest_comparison, amount_strategy_metrics
- [ ] Добавить в презу суммы
- [ ] Перечитать текст, чтобы все мне там было понятно
- [ ] Попросить Куренного еще раз выступить

--- P1 (ВАЖНЫЕ) ---
- [x] Заменить `$$...$$` на `\[...\]` в формуле PD во Введении (document.tex, ~строка 147)
- [x] Исправить дублирование \bibitem{crosbie2003} в библиографии (два определения с одним ключом)
- [ ] Обсудить в тексте низкий R2=0.188 пулированной OLS-регрессии (объясняющая сила)
- [ ] Обсудить незначимость Грэнджер-тестов для 3 из 4 макропеременных (contemporaneous vs. predictive)
- [ ] Переименовать _solve_merton_vectorized -> _solve_merton_iterative (это НЕ векторизация)
- [x] requirements.txt настроить, чтобы только то что нужно было в среде (убран lxml, добавлены linearmodels, prophet, ipywidgets; tqdm переключён на tqdm.auto для красивых notebook-баров)
- [ ] Перевести на английский комментарии в main

--- P2 (УЛУЧШЕНИЯ) ---
- [ ] Переписать Заключение с конкретными числовыми результатами (таблица)
- [ ] Добавить 10-15 источников: Базель II/III, CreditMetrics, Lando (2004), российские авторы
- [ ] Добавить формальный раздел "Постановка задачи" перед Главой 1
- [ ] Описать в тексте источник мультипликаторов (Smart-Lab) и обработку пропусков
- [ ] Вместо warnings.filterwarnings("ignore") фильтровать конкретные предупреждения в main.ipynb
- [ ] Обсудить ограничение: LGD = const = 0.4 в weight-based стратегиях vs. индивидуальный LGD в amount-based
- [ ] Поправить readme.md
- [ ] Названия и пути графиков унифицировать

--- ОТЛАДКА: _solve_merton_vectorized_fn (data_layer.py) ---
- [ ] Входная валидация NaN: если E, D, r или sigma_E = NaN — пропускать строку, ставить V=NaN, sigma_V=NaN вместо решения мусора
- [ ] Обработка D=0: если долга нет — компания не может дефолтнуть; ставить PD=0, DD=+inf, V=E, sigma_V=sigma_E напрямую без солвера

--- ИДЕИ ---
- [ ] Учесть государственную поддержку компаний: для госкомпаний (Газпром, Роснефть, Сбербанк, ВТБ и др.) implicit government guarantee снижает реальную PD; можно ввести поправочный коэффициент или отдельный фактор

--- ОТЛАДКА: create_portfolio_fn (data_layer.py) ---
- [ ] np.select: второй choice возвращает NaN-столбец "Долг" (который isna по условию) — заменить на явный 0 или np.nan
- [ ] .drop(columns=[...]) без errors="ignore" — упадёт если Smart-Lab изменит формат CSV
- [ ] *=BILLION в цикле не идемпотентен — повторный вызов удвоит значения; добавить guard
- [ ] capitalization.fillna(0) опасно для Мертона: E=0 вырождает систему уравнений
- [ ] debt.fillna(0) + EPSILON-защита в решателе даёт DD=ln(V/1e-6)=+∞ → PD≈0; рассмотреть минимальный порог
- [ ] interest_rate не заполняется ffill/bfill (inflation и unemployment заполняются) — NaN потечёт в Мертон
- [ ] replace(0, NaN) нивелируется последующим fillna(0) для строк без валидных соседей

--- ОТЛАДКА: add_dynamic_features_fn (data_layer.py) ---
- [ ] global_avg_vol — среднее по ВСЕМ тикерам; fallback может быть неадекватен для отдельных компаний; рассмотреть by-sector среднее
- [ ] log_return вычисляется и удаляется, а в optimization.py пересчитывается заново — можно оставить в portfolio DF и переиспользовать

--- ОТЛАДКА: forecast_macro_fn / predict_macro_factors_fn / predict_dd_fn / predict_pd_fn (credit_risk.py) ---
- [x] (1) Двойной пересчёт макро-прогноза: добавлен опциональный параметр macro_forecast в predict_dd_fn/predict_pd_fn; forecast_macro_fn передаёт готовый прогноз вниз. Время 20.49s -> 6.81s (x3), prophet 17.76s -> 5.24s. Значения DD не изменились до 4-го знака.
- [x] (2) VAR: реализован выбор лага по BIC через VAR.select_order(maxlags=min(12, n//4)); добавлено логирование выбранного лага.
- [x] (3) SARIMAX: вместо фиксированного order=(1,d,0) реализован AIC-перебор (p,q) по сетке {0,1,2}^2 \ {(0,0)}; сезонная часть оставлена пустой (n_obs ~75 недостаточно для надёжной идентификации). Текст диплома обновлён: ARIMAX(p,d,q), AIC-выбор, объяснение отсутствия сезонности и динамические экзогены через VAR.
- [x] (4) Prophet: добавлены add_regressor() для всех остальных макрофакторов; их будущие значения берутся из вспомогательного VAR-прогноза (как в SARIMAX). Текст диплома обновлён.
- [x] (5) predict_dd_fn/predict_pd_fn теперь сохраняют полную траекторию в self.d["dd_forecast_path"]/"pd_forecast_path"; forecast_macro_fn вызывает predict_*_fn ОДИН раз на H шагов и берёт среднее по тикерам — вместо H повторных OLS-фитов. Убрана семантическая мешанина пошагового и многошагового прогнозов.
- [ ] (6) OLS контемпоральная: DD_t = f(M_t), без лагов. Не предиктивная, а синхронная регрессия.
- [x] (7) DRY: predict_pd_fn и predict_dd_fn слиты в общий _predict_target_fn(target='PD'|'DD'). Старые имена сохранены как тонкие обёртки. Дублирование ~85 строк убрано. predict_dd_fn и predict_pd_fn дублируют ~95% кода; вынести в общую функцию с параметром target.
- [ ] (8) predict_dd_fn/predict_pd_fn пересчитывают macro_df, pivot, OLS на каждой итерации цикла — лишняя работа.
- [ ] (9) is_backtest = horizon < 0: перегрузка одного параметра; добавить явный backtest: bool.
- [ ] (10) Магические константы (maxiter=500, changepoint_prior_scale=0.05, min(3,...)) — вынести в cfg или обосновать.
- [ ] (11) SARIMAX: enforce_stationarity=False, enforce_invertibility=False глушит warnings без анализа причин.
- [ ] (12) OLS: нет train/test, нет диагностики остатков (Durbin-Watson, гетероскедастичность).
- [x] (13) SARIMAX exog_fc: вместо повторения последнего значения экзогенов H раз — реализован динамический прогноз через вспомогательный VAR(BIC) на той же макропанели.
- [x] (14) Клипирование PD теперь консистентно: в PD-ветке clip(0,1) применяется и к скалярному predicted_pd, и к pd_forecast_path. В DD-ветке predicted_pd производится через norm.cdf(-DD), который и так возвращает [0,1] — клип не нужен. Несогласованный clip PD: predict_pd_fn делает clip(0,1), predict_dd_fn пересчитывает PD через norm.cdf без clip.
